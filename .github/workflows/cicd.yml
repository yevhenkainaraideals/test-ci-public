name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:  # Allows manual trigger

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build .NET App
        run: dotnet build --configuration Release

  deploy:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        environment: [qa, uat, prod]
    environment: ${{ matrix.environment }}  # Triggers GitHub Environment approvals
    uses: yevhenkainaraideals/github-pipeline-templates-public/.github/workflows/deploy.yml@main
    with:
      aws_region: ${{ vars.AWS_REGION }}
      resource_prefix: ${{ vars.RESOURCE_PREFIX }}
    secrets:
      AWS_SECRET: ${{ secrets.AWS_SECRET }}
